FEATURE: F0.1-resolve-inflight-work
BRANCH: mission/etl-l5/F0.1-resolve-inflight-work
COMMITS:
  008a969 Add sema rich-type helpers, etl_input runtime, and scripted-input example
  4e18042 Update docs for sema-rich and scripted-input milestone
  61ce52a Wire scripted_input_smoke.sh and update equiv/visual smoke scripts
CHANGED FILES:
  compiler1/sema.etl
  compiler1/README.md
  runtime/etl_input.c
  runtime/etl_input.h
  examples/visual/scripted_input.etl
  examples/visual/scripted_input.events
  examples/visual/scripted_input.expected
  tests/c1_corpus/extern_typed_write.etl
  docs/PATH_TO_APPS.md
  docs/DESIGN.md
  docs/ROADMAP.md
  docs/SPEC.md
  docs/backend-plan.md
  docs/c1-corpus-expansion-plan.md
  scripts/scripted_input_smoke.sh
  scripts/c1_equiv_smoke.sh
  scripts/visual_smoke.sh
GATES RUN:
  scripts/scripted_input_smoke.sh: 0
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  make selfhost: 0
  make selfhost-selfcompile: 0
  make backend-vm: 0
SUCCESS STATE:
  All uncommitted in-flight work was committed cleanly on the feature branch. The
  scripted-input smoke (scripts/scripted_input_smoke.sh) passed: it compiles
  examples/visual/scripted_input.etl via compiler-0, links runtime/etl_input.c,
  runs the binary against examples/visual/scripted_input.events, and diffs stdout
  against examples/visual/scripted_input.expected (golden: "10042"). The sema.etl
  additions introduce sema_type_is_i32_array, sema_type_is_struct_array_decl, and
  sema_type_to_scalar_ty — helpers that allow the sema pass to classify typed extern
  parameters (e.g. i8[128] path arguments). All six gates are green. Tree is clean
  except .local/ (intentionally untracked).
WHAT WAS LEFT UNDONE:
  - Merge to main is deferred to F0.2 as specified.
  - make selfhost-bootstrap and make examples-cli / make visual / make examples were
    not required by this feature's VAL-IDs and were not run; F0.2/F0.3 should gate
    those before merging to main.
DISCOVERED ISSUES:
  None. All tests passed without modification to the in-flight work.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F0.1-resolve-inflight-work.md
