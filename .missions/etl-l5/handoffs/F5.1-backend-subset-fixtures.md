FEATURE: F5.1-backend-subset-fixtures
BRANCH: mission/etl-l5/F5.1-backend-subset-fixtures
COMMITS:
  278e482 F5.1: extend backend-subset to four backends (C, VM, ASM, WAT)
  2101e56 Merge mission/etl-l5/F5.1-backend-subset-fixtures: four-backend subset smoke (VAL-BE-001)
CHANGED FILES:
  scripts/backend_subset_smoke.sh
  docs/backend-plan.md
GATES RUN:
  scripts/backend_subset_smoke.sh: 0
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  make backend-subset: 0
  make selfhost: 0
  make examples: 0
SUCCESS STATE:
  backend_subset_smoke.sh now drives all four compiler-1 backends (C, VM
  bytecode, ASM, WAT) for each fixture. A shared run_vm C harness is built
  once against runtime/etl_vm.c; each fixture emits bytecode via
  emit_bytecode, writes to a temp file, and the harness executes it through
  etl_vm_run_main_i32. Sixteen fixtures pass all four backends (hard floor ≥10
  satisfied). Two fixtures (ret_logical, eager_and_truthy) use or/and binary
  ops that emit_bytecode does not emit and etl_vm.c does not dispatch; they
  run as three-backend (C/ASM/WAT) cases via run_case_3. docs/backend-plan.md
  updated with a "Backend subset" section listing the full fixture table,
  exclusion reasons, and candidate pool summary. make backend-subset exits 0.
WHAT WAS LEFT UNDONE:
  and/or opcodes not added to emit_bytecode or etl_vm.c — doing so would
  bring eager_and_truthy and ret_logical into the four-backend set, but it
  is a separate backend feature not required by VAL-BE-001 to reach 16
  passing fixtures.
DISCOVERED ISSUES:
  emit_bytecode has no case for TK_AND or TK_OR in its binary-op dispatch
  (falls through to EMIT_ERR_UNSUPPORTED). etl_vm.c dispatch loop has no
  matching opcode character for and/or. Both gaps are documented in the
  backend-plan.md exclusion table.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F5.1-backend-subset-fixtures.md
