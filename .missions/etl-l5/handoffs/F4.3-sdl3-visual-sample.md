FEATURE: F4.3-sdl3-visual-sample
BRANCH: mission/etl-l5/F4.3-sdl3-visual-sample
COMMITS:
  fa48b84 F4.3: bouncing_rect SDL3 visual sample with golden PPM and smoke gate
  673dce3 Merge mission/etl-l5/F4.3-sdl3-visual-sample: SDL3 visual sample with bouncing_rect golden frame
CHANGED FILES:
  Makefile
  examples/visual/bouncing_rect.etl
  examples/visual/bouncing_rect.golden.ppm
  scripts/sdl3_visual_smoke.sh
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  scripts/sdl3_visual_smoke.sh: 0
  make sdl3-visual: 0
  make visual: 0
  make examples: 0
SUCCESS STATE:
  Shipped bouncing_rect.etl — a 30-frame deterministic bouncing-rect demo
  (8x8 yellow rect on 32x32 dark-blue canvas) that compiles through compiler0
  to C, links against live SDL3 from .deps/sdl3/, and runs headlessly via
  SDL_VIDEODRIVER=offscreen. The final frame (frame 30) is written as
  build/graphics/bouncing_rect.ppm; the golden copy is committed at
  examples/visual/bouncing_rect.golden.ppm (3085 bytes, P6 32x32 PPM).
  scripts/sdl3_visual_smoke.sh rebuilds from source, runs live SDL3, validates
  the center pixel (0xFFFF00 = yellow, value 16776960), and byte-compares
  the output PPM against the golden — exits 0 on match, 1 on mismatch.
  make sdl3-visual target wired. VAL-SDL3-003 satisfied.
WHAT WAS LEFT UNDONE:
  Conway's Life (Phase 6e) deferred to F4.4 as planned.
  SDL_image / PNG output not pursued — PPM P6 is sufficient and dependency-free.
  The existing make visual / visual_smoke.sh still reports "SKIP sdl3 (SDL3 not
  installed)" because it uses pkg-config detection; the new sdl3_visual_smoke.sh
  uses .deps/sdl3/ directly and is the authoritative SDL3 gate for this feature.
DISCOVERED ISSUES:
  'size' is a reserved ETL keyword (aliased to sizeof); renamed the draw_rect
  parameter to 'rlen' to avoid the parse error.
  Comments with '#' are not valid ETL syntax; source uses no comments.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F4.3-sdl3-visual-sample.md
