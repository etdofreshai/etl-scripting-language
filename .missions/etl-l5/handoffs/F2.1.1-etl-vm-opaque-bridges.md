FEATURE: F2.1.1-etl-vm-opaque-bridges
BRANCH: mission/etl-l5/F2.1.1-etl-vm-opaque-bridges
COMMITS:
  ad54e06 F2.1.1: wire HS*/HD*/HV* opaque ops to real runtime via vm_bridge
  9ec5125 Merge mission/etl-l5/F2.1.1-etl-vm-opaque-bridges into main
CHANGED FILES:
  Makefile
  compiler1/vm.etl
  runtime/vm_bridge.c
  scripts/c1_etl_vm_smoke.sh
GATES RUN:
  make bin/etl-vm-etl: 0
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  make selfhost: 0
  make selfhost-selfcompile: 0
  make selfhost-bootstrap: 0
  make backend-vm: 0
  scripts/c1_etl_vm_smoke.sh (extended, 7 fixtures): 0
  valgrind bin/etl-vm-etl < string_heap_basic.bc: 0 errors / 0 definitely-lost
  valgrind bin/etl-vm-etl < dynarr_basic.bc: 0 errors / 0 leaks
SUCCESS STATE:
  runtime/vm_bridge.c adds a C-side handle-table trampoline layer (64-slot
  tables for EtlString*, EtlDynArr*, EtlVal*, and raw void* heap pointers).
  compiler1/vm.etl now declares extern fn for all bridge functions and calls
  vm_ha_alloc/vm_ha_free, vm_hs_*/vm_hd_*/vm_hv_* in each HS*/HD*/HV* opcode
  body, replacing the stack-discipline-only stubs from F2.1. The HVA? two-char
  sub-ops (as_int/as_bool/as_ptr/as_str) are fully dispatched. Makefile links
  etl_string.c, etl_dynarr.c, etl_etlval.c, and vm_bridge.c into etl-vm-etl.
  Smoke script extended with fixtures 5-7 (string_heap_basic, dynarr_basic,
  tagged_union_basic) all exiting 0 via the ETL VM. Valgrind clean.
WHAT WAS LEFT UNDONE:
  etlval_str variant (HVS) is wired but the fixture (tagged_union_basic) does
  not exercise it end-to-end with etlval_as_str round-trip. A dedicated
  tagged_union_str fixture would be needed to fully validate HVAS. Deferred
  to F2.2 or a sub-feature if the str round-trip proves needed for equivalence.
  The HVA? as_ptr path aliases the pointer into a new ha_tab slot without
  tracking ownership — callers must not double-free the underlying memory.
  This is documented in vm_bridge.c and acceptable for the current fixture set.
DISCOVERED ISSUES:
  ETL's i8 local scalars cannot be initialized with integer literal 0 (type
  mismatch vs i32). Worked around by using i32 for the sub3 dispatch variable.
  The "still reachable: 8 bytes" in valgrind for string_heap_basic is the raw
  etl_alloc(8) block freed via vm_ha_free — valgrind reports 0 errors. Root
  cause: the fixture calls etl_free(p) via the bytecode HA path, which routes
  through vm_ha_free; the EtlString copy made by str_new is freed separately
  by vm_hs_free. The 8 bytes "still reachable" trace to etl_runtime.c globals,
  not the bridge.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F2.1.1-etl-vm-opaque-bridges.md
