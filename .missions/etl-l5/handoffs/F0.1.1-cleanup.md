FEATURE: F0.1.1-cleanup
BRANCH: mission/etl-l5-root
COMMITS:
  59429cc Move mission vendor deps target from ./bin/ to ./.deps/
  34b1840 Add make clean-mission target
CHANGED FILES:
  scripts/setup.sh
  .gitignore
  Makefile
  .missions/etl-l5/validation-contract.md
  .missions/etl-l5/features.json
  .missions/etl-l5/mission-plan.md
GATES RUN:
  bash scripts/setup.sh: 0
  git check-ignore .deps/anything: 0
  make clean-mission: 0
  make check: 0
SUCCESS STATE:
  Vendor dependency downloads moved from ./bin/ to ./.deps/ across all mission
  artifacts (scripts/setup.sh renamed setup_bin->setup_deps, .gitignore swapped
  bin/ for .deps/, validation-contract.md VAL-SDL3-001/VAL-BE-003/VAL-DIST-002/
  VAL-DIST-004/VAL-REL-003 updated, features.json and mission-plan.md updated).
  The tracked bin/etl CLI wrapper is untouched. A new `make clean-mission` target
  removes runtime/test_runtime, build/fixedpoint/, and .deps/ without touching
  any tracked file. VAL-FND-003 now documents that `make visual` may exit 0 with
  SKIP when SDL3 is absent, and that VAL-SDL3-* requires SDL3 live in .deps/sdl3/.
  The merged F0.1 feature branch was deleted (no unique commits). The long-lived
  mission/etl-l5-root branch was created to host milestone-bridging structural work.
  make check exits 0 throughout.
WHAT WAS LEFT UNDONE:
  None — all steps in the F0.1.1-cleanup spec were completed.
DISCOVERED ISSUES:
  None.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F0.1.1-cleanup.md
