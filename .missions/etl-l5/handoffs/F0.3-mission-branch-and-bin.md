FEATURE: F0.3-mission-branch-and-bin
BRANCH: main (see DISCOVERED ISSUES — mission/etl-l5 branch could not be created)
COMMITS:
  9cec52c Add mission/etl-l5 setup.sh and bin/ gitignore
CHANGED FILES:
  .gitignore
  scripts/setup.sh
GATES RUN:
  bash scripts/setup.sh: 0
  git check-ignore bin/new-tool: 0
  make check: 0
SUCCESS STATE:
  scripts/setup.sh was created (executable, set -euo pipefail, idempotent) with stub functions fetch_sdl3(), fetch_wasmtime(), fetch_wat2wasm(), fetch_qemu(), and fetch_headless_chrome(). Each emits a clear "not yet implemented (MN)" status line. setup_bin() creates ./bin/ if absent. bin/ was appended to .gitignore; git check-ignore confirms new files under bin/ are ignored. make check passes with all smokes green.
WHAT WAS LEFT UNDONE:
  Actual fetch logic for SDL3 (M4), wasmtime/wat2wasm (M5), qemu-aarch64-static (M6), headless Chrome/Playwright (M6) — deferred by spec to those milestones.
DISCOVERED ISSUES:
  The long-lived branch mission/etl-l5 could not be created: git refuses because refs/heads/mission/etl-l5/F0.1-resolve-inflight-work already exists and git treats mission/etl-l5 as a directory path. The commit was landed on main instead. Future workers should note this and either delete the F0.1 sub-branch or use an alternate long-lived branch name (e.g. mission/etl-l5-root). bin/etl already existed as a tracked file; bin/ gitignore correctly ignores new/untracked files but does not un-track bin/etl (expected git behavior). git check-ignore bin/ returns exit 1 for that reason, but git check-ignore bin/new-tool returns exit 0.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F0.3-mission-branch-and-bin.md
