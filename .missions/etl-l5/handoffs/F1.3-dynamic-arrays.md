FEATURE: F1.3-dynamic-arrays
BRANCH: mission/etl-l5/F1.3-dynamic-arrays
COMMITS:
  4beadb3 Add dynarr opaque type and HD* VM opcodes (F1.3-dynamic-arrays part a/b/c)
CHANGED FILES:
  Makefile
  compiler1/emit_bytecode.etl
  compiler1/emit_c.etl
  compiler1/sema.etl
  compiler1/test_emit_c.etl
  runtime/etl_dynarr.c
  runtime/etl_dynarr.h
  runtime/etl_vm.c
  scripts/c1_dynarr_equiv_smoke.sh
  scripts/c1_emit_c_smoke.sh
  scripts/c1_runtime_compile_smoke.sh
  scripts/c1_string_equiv_smoke.sh
  scripts/c1_vm_control_flow_smoke.sh
  scripts/c1_vm_expr_smoke.sh
  scripts/c1_vm_function_smoke.sh
  scripts/c1_vm_heap_alloc_smoke.sh
  scripts/c1_vm_return_smoke.sh
  scripts/c1_vm_string_smoke.sh
  scripts/heap_alloc_valgrind_smoke.sh
  tests/c1_corpus/dynarr_basic.etl
  tests/c1_corpus/dynarr_grow.etl
  tests/c1_corpus/dynarr_set.etl
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  scripts/c1_dynarr_equiv_smoke.sh: 0
  make selfhost: 0
  make backend-vm: 0
  scripts/heap_alloc_valgrind_smoke.sh: 0
SUCCESS STATE:
  Shipped the dynarr opaque scalar type backed by EtlDynArr* (runtime/etl_dynarr.c/h). The C backend emits EtlDynArr* locals/params/returns and includes etl_dynarr.h in the preamble. The VM backend handles HD* opcodes (HDN/HDP/HDL/HDG/HDS/HDF) via a per-call dynarr_table[64] mirroring the HS* string pattern. Three corpus fixtures (dynarr_basic, dynarr_grow, dynarr_set) pass byte-for-byte equivalent exit codes on both C and VM backends, validated by scripts/c1_dynarr_equiv_smoke.sh. Valgrind reports 0 errors and 0 leaks on all three fixtures. All pre-existing gates remain green. Branch was built on top of F1.2-mutable-strings (inheriting F1.1 and F1.2). Rebase choice: branched directly from mission/etl-l5/F1.2-mutable-strings, picking up both prior features without a merge commit.
WHAT WAS LEFT UNDONE:
  Generic element types (non-i32 dynarr) are not implemented — i32-only per the spec. No iterator protocol, no map/filter, no ETL syntax changes. dynarr_new() takes no arguments in ETL source (C backend correctly emits dynarr_new() call with no params).
DISCOVERED ISSUES:
  Adding #include <etl_dynarr.h> to the emit_c preamble changed the total byte count of emitted C from 110 to 134 for the canonical test_emit_c.etl fixture, requiring updates to compiler1/test_emit_c.etl and scripts/c1_emit_c_smoke.sh. All VM smoke scripts that compile etl_vm.c also required etl_dynarr.c added to their link lines. No VM-can't-read-literal-data issues arose (dynarr ops involve no literal data in the bytecode stream).
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F1.3-dynamic-arrays.md
