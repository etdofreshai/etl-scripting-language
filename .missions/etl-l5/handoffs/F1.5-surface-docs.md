FEATURE: F1.5-surface-docs
BRANCH: mission/etl-l5/F1.5-surface-docs
COMMITS:
  2051347 docs: add M1 opaque-type surface to SPEC and DESIGN (F1.5-surface-docs)
  11f3754 docs: update support-matrix with M1 opaque-type rows (F1.5-surface-docs)
  e926d9d docs: add M1 typed extern section to compiler1/README (F1.5-surface-docs)
CHANGED FILES:
  docs/SPEC.md
  docs/DESIGN.md
  docs/support-matrix.md
  compiler1/README.md
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0 (34/34 passed)
SUCCESS STATE:
  docs/SPEC.md now contains a new section "Opaque types (M1 capabilities — v0)"
  describing ptr, str, dynarr, and etlval with their full extern surfaces,
  accurate C-backend and VM-backend behavior, and all known limitations:
  compiler-0 unsupported, VM str_new creates empty string, 1024-byte BC buffer
  tech debt, dynarr i32-only, etlval str-variant fixture elided, ASM/WAT
  not yet supported. Section is inserted between the external function section
  and the Operator precedence section.

  docs/DESIGN.md has a new section 37b "M1 Opaque-Type Design Rationale (v0)"
  explaining the extern-call-surface approach (no new syntax), runtime heap
  ownership, VM handle-table design (HS*/HD*/HV* opcode families, 64-slot
  bounded tables), VM str_new limitation rationale, bytecode buffer tech debt,
  and the intentional scope boundary of not extending compiler-0.

  docs/support-matrix.md has four new rows in the language feature table
  (ptr, str, dynarr, etlval) with column statuses c0=UNSUPPORTED, c1/C=SUPPORTED,
  ASM=UNSUPPORTED, WAT=UNSUPPORTED, VM=SUPPORTED-with-noted-limits. The
  Limitations section was updated to replace stale "no heap string type" and
  "no dynamic array type" claims with accurate M1 descriptions including the
  dynarr i32-only constraint, VM bytecode buffer tech debt, VM str_new
  empty-string behavior, and ASM/WAT non-support.

  compiler1/README.md has a new section "M1 typed extern support" with a table
  of all four M1 types, their internal tags (TY_PTR/TY_STR/TY_DYNARR/TY_ETLVAL),
  C backend emission types, and VM opcode families. The emit_c.etl row in the
  file inventory was updated to mention M1 opaque-type externs. The smoke test
  section was updated to reference the three dedicated M1 equivalence smoke scripts.

  docs/PATH_TO_APPS.md: no changes needed. The existing statement that
  "runtime-facing capability reaches ETL through extern fn declarations and
  C-side runtime implementations" is already accurate and consistent with M1.

  Both make check (exit 0) and scripts/c1_equiv_smoke.sh (exit 0, 34/34) were
  confirmed green after all doc edits. No source code was modified.
WHAT WAS LEFT UNDONE:
  - docs/ROADMAP.md and docs/language-goal-roadmap.md were not updated (the
    skill audit list includes them but the feature spec did not call for changes
    there, and the audited content was already accurate or aspirational).
  - docs/backend-plan.md, docs/runtime-vm-plan.md, docs/c1-corpus-expansion-plan.md,
    and docs/fixed-point-plan.md were audited but not changed (content is either
    accurate or appropriately aspirational / future-scoped; no false claims found
    that needed correction for the M1 surface).
DISCOVERED ISSUES:
  - SPEC.md section on `pointer` (line 206 in original) states the C backend
    emits `pointer` as `int8_t *`. However, compiler-1's TY_PTR (F1.1 handoff)
    emits `void*`, not `int8_t *`. The SPEC description matches the compiler-0
    Python behavior, not compiler-1. This is a pre-existing docs/code divergence
    on the base `pointer` type, predating M1. Filing for orchestrator scoping;
    not fixed in this docs-only feature.
  - SPEC.md "Compiler-1 (in ETL)" section (line 295 original) still describes
    compiler-1 as a "skeleton" with "placeholder modules with trivial functions,
    not yet linked into the build." This is stale; compiler-1 is a full working
    compiler at fixed point. Pre-existing stale claim; not changed to avoid
    touching aspirational/historical narrative text. Recommend a separate docs
    pass to update the Compiler-1 current-state description.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F1.5-surface-docs.md
