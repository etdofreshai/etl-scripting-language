FEATURE: F2.3-triple-equivalence
BRANCH: mission/etl-l5/F2.3-triple-equivalence
COMMITS:
  b31ab2c F2.3: add triple-equivalence smoke gate (≥20 fixtures, c0/C + c1/C + c1-VM)
  a66c29e Merge mission/etl-l5/F2.3-triple-equivalence: add triple-equivalence gate
CHANGED FILES:
  scripts/triple_equiv_smoke.sh
  Makefile
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  make selfhost: 0
  make backend-vm: 0
  make vm-equivalence: 0
  scripts/triple_equiv_smoke.sh: 0
SUCCESS STATE:
  scripts/triple_equiv_smoke.sh is added and passes with exactly 20 fixtures achieving
  triple equivalence (c0/C exit == c1/C exit == c1-bytecode→ETL-VM exit). The Makefile
  gains targets `triple-equivalence` (depends on bin/etl-vm-etl, runs the smoke) and
  `backend-vm-triple` (runs backend-vm then triple-equivalence). Candidate pool was all
  44 fixtures in tests/c1_corpus/; 20 passed all three pipelines; 24 were excluded.
  Fixture counts: candidate pool=44, included=20, excluded=24.

  Excluded fixtures and reasons:
    emit_bytecode limitation (bc compilation fails):
      ret_unary_minus    — emit_bytecode does not support unary-minus literal
      local_bool         — emit_bytecode does not support bool local type
      local_i8           — emit_bytecode does not support i8 scalar local
      local_i8_array     — emit_bytecode does not support i8 array indexing
      local_array_sum    — emit_bytecode does not support array sum ops
      local_array_loop   — emit_bytecode does not support array loop ops
      i32_array_param    — emit_bytecode does not support i32 array params
      if_logical_and     — emit_bytecode does not support && short-circuit ops
      if_logical_or      — emit_bytecode does not support || short-circuit ops
      full_word_aliases  — emit_bytecode does not support keyword-alias syntax

    c0 compiler type-support gap (python3 -m compiler0 fails):
      struct_decl        — c0 does not support struct types
      field_access_fn    — c0 does not support struct types
      struct_array       — c0 does not support struct types
      string_local       — c0 does not support string types
      string_multi       — c0 does not support string types
      extern_typed_write — c0 does not support string types
      string_heap_basic  — c0 does not support heap string ops
      string_heap_concat — c0 does not support heap string ops
      string_heap_eq     — c0 does not support heap string ops
      dynarr_basic       — c0 does not support dynamic array type
      dynarr_grow        — c0 does not support dynamic array type
      dynarr_set         — c0 does not support dynamic array type
      tagged_union_basic    — c0 does not support tagged union type
      tagged_union_dispatch — c0 does not support tagged union type

WHAT WAS LEFT UNDONE:
  Expanding the fixture count beyond 20 would require either:
  (a) implementing missing emit_bytecode ops (unary-minus, bool local, i8 types,
      array indexing, logical-and/or, aliases) — deferred to future Fx features; or
  (b) extending c0 to support struct/string/dynarr/tagged-union types — out of scope
      for M2.
DISCOVERED ISSUES:
  None. All 20 included fixtures produced identical exit codes across all three
  pipelines on first run. nested_let_block was newly confirmed triple-equivalent
  (not previously in vm_equivalence_smoke).
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F2.3-triple-equivalence.md
