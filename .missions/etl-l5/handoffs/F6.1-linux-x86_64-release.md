FEATURE: F6.1-linux-x86_64-release
BRANCH: mission/etl-l5/F6.1-linux-x86_64-release
COMMITS:
  644c503 F6.1: add Linux x86_64 release tarball and smoke gate (VAL-DIST-001)
  e735029 Merge mission/etl-l5/F6.1-linux-x86_64-release: Linux x86_64 release tarball (VAL-DIST-001)
CHANGED FILES:
  Makefile
  scripts/build_release_tarball_x86_64.sh
  scripts/release_smoke_x86_64.sh
GATES RUN:
  make check: 0
  make selfhost: 0
  make release-check: 0
  scripts/build_release_tarball_x86_64.sh (standalone): 0
  scripts/release_smoke_x86_64.sh (standalone): 0
SUCCESS STATE:
  Shipped `make release-tarball-x86_64` (via `scripts/build_release_tarball_x86_64.sh`) which stages bin/etl (self-contained sh launcher), bin/etl-vm-etl (pre-built, glibc-only), bundled compiler0 Python module, compiler1 ETL sources, runtime C sources, and minimal docs into build/release/etl-linux-x86_64/, then reproducibly tar+gzips to build/release/etl-linux-x86_64.tar.gz. `scripts/release_smoke_x86_64.sh` untars to a temp dir, compiles `fn main() i32 ret 42 end` with the bundled etl CLI (setting PYTHONPATH to find the bundled compiler0), and verifies exit 42. `make release-check` now includes `release-check-x86_64` as its final step. VAL-DIST-001 is satisfied.
WHAT WAS LEFT UNDONE:
  Static linking of the etl-vm-etl binary beyond glibc is not attempted (only glibc is needed, so not required). The smoke test requires python3 and cc (C11) on the target machine; the constraint "no system deps beyond glibc" applies to shared libraries, not build toolchain, consistent with the ETL CLI design.
DISCOVERED ISSUES:
  python3 -m compiler0 in etl_cli.sh requires the working directory or PYTHONPATH to contain compiler0/. The tarball launcher sets PYTHONPATH="$ETL_ROOT" to make this work without patching etl_cli.sh itself.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F6.1-linux-x86_64-release.md
