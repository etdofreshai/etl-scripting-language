FEATURE: F5.4-support-matrix-refresh
BRANCH: mission/etl-l5/F5.4-support-matrix-refresh
COMMITS:
  6e104be F5.4: refresh support-matrix against actual fixture state (VAL-BE-004)
  841749b Merge mission/etl-l5/F5.4-support-matrix-refresh: support-matrix refresh (VAL-BE-004)
CHANGED FILES:
  docs/support-matrix.md
GATES RUN:
  make check: 0
SUCCESS STATE:
  docs/support-matrix.md is refreshed against the actual fixture state as of
  F5.1 (backend-subset) and F5.3 (backend-wasm-wasmtime). Key corrections made:

  1. VM column accuracy:
     - comparisons (==,!=,<,<=,>,>=): changed from UNSUPPORTED to ✓ SUPPORTED.
       All six operators proven by cmp_eq, cmp_neq, cmp_lte, cmp_gt, cmp_gte,
       cmp_false in the 16 four-backend shared fixture set.
     - logical `not` (unary): changed from UNSUPPORTED to ✓ SUPPORTED.
       ret_not_true and ret_not_false are in the 16 four-backend set.
     - `if`/`elif`/`else`: changed from UNSUPPORTED to △ PARTIAL.
       if/else proven (if_then_local, if_else_local); elif in VM not confirmed.
     - `while`: changed from UNSUPPORTED to ✓ SUPPORTED.
       while_count is in the 16 four-backend set.
     - `fn` declaration (single function): changed from UNSUPPORTED to ✓ SUPPORTED.
       All 16 four-backend fixtures use `fn main() i32`.
     - `return`: changed from EXPERIMENTAL to ✓ SUPPORTED via backend-subset.

  2. logical `and`/`or` separated from `not`:
     - and/or: VM explicitly ✗ UNSUPPORTED with documented reason (emit_bytecode
       has no TK_AND/TK_OR opcode; no matching etl_vm.c dispatch case).
     - Two three-backend fixtures (ret_logical, eager_and_truthy) documented as
       C/ASM/WAT only.

  3. emit_bytecode gap list added to Limitations section: unary-minus literal,
     bool/i8 scalar locals, i8 array indexing, logical and/or, general array ops
     (sum/loop/i32 array params), and keyword-alias syntax. These match the F2.3
     triple-equiv exclusion list exactly.

  4. backend-wasm gate note updated: 23 cases via wasmtime + 2 extern cases via
     wat2wasm validation only. WASI exit-code range limitation added (wasmtime v36
     clamps [0..126)). WAT extern-call validation-only limitation added.

  5. Optional dependencies updated with pinned versions: wasmtime v36.0.9 and
     wabt 1.0.41 (from scripts/setup.sh).

  6. Grid reformatted to ✓/△/✗ notation for clarity. Column order changed to
     c0/C, c1/C, c1/VM, c1/ASM, c1/WAT to show the VM column distinctly.

  make check passes (103 ok/PASS; no failures).

WHAT WAS LEFT UNDONE:
  - elif support in VM is not confirmed by any fixture. Marked △ for if/elif/else
    VM column. A dedicated elif-in-VM fixture would resolve this either way.
  - WAT extern void-return case is still validation-only. The WAT extern-call
    smoke limitation is documented but not addressed.
  - fn params i32 in VM: no multi-function-with-i32-params fixture exists for VM;
    marked ✗ pending a fixture.

DISCOVERED ISSUES:
  The prior support-matrix made several aspirational claims that contradicted the
  actual fixture record:
  - comparisons, if/elif/else, while, fn declaration, and return were all marked
    UNSUPPORTED in VM despite being proven by the 16 four-backend fixtures in F5.1.
  - logical and/or/not were lumped in one row; `not` IS supported in VM but
    `and`/`or` are not. The single-row treatment obscured the real state.
  None of these require code changes; they were documentation errors only.

PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F5.4-support-matrix-refresh.md
