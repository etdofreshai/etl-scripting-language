FEATURE: F1.1.2-fix-selfhost-regression
BRANCH: mission/etl-l5/F1.1.2-fix-selfhost-regression
COMMITS:
  d5bf956 Fix selfhost-selfcompile/selfhost-bootstrap include path for M1 runtime headers
  00f7c8b Merge mission/etl-l5/F1.1.2-fix-selfhost-regression
CHANGED FILES:
  scripts/c1_selfcompile_smoke.sh
GATES RUN:
  make selfhost-selfcompile (pre-fix, reproduce): 1
  make selfhost-selfcompile (post-fix): 0
  make selfhost-bootstrap: 0
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  make selfhost: 0
  make backend-vm: 0
  make examples-cli: 0
  git status --porcelain: 0 (clean)
SUCCESS STATE:
  The regression on VAL-FND-003 is closed. The root cause was that
  scripts/c1_selfcompile_smoke.sh line 176 invoked cc without -I runtime,
  so the emitted C's unconditional #include <etl_string.h> (added by the
  M1 emit_c preamble) caused a fatal error. Adding -I runtime to that single
  cc invocation resolved the include path. The c1_bootstrap_smoke.sh c3/c4
  cc stages already carried -I runtime; only the c2/selfcompile stage was
  missing it. After the fix, make selfhost-selfcompile and make
  selfhost-bootstrap both exit 0 and achieve three-stage fixed-point
  (sha=60265cdc6063). All other gates remain green post-merge into main.
WHAT WAS LEFT UNDONE:
  None. Fix is minimal and complete.
DISCOVERED ISSUES:
  The status-message echo on line 195 of c1_selfcompile_smoke.sh (the
  diagnostic string logged to selfcompile-status.md on cc failure) was also
  updated to reflect the -I runtime flag, keeping the error report accurate
  for future failures.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F1.1.2-fix-selfhost-regression.md
