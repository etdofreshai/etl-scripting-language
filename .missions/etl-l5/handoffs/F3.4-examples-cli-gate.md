FEATURE: F3.4-examples-cli-gate
BRANCH: mission/etl-l5/F3.4-examples-cli-gate
COMMITS:
  a3d9561 F3.4: close M3 tech debt — fix LIMITATION comment and remove unused helpers
  9c66e86 Merge mission/etl-l5/F3.4-examples-cli-gate: verify examples-cli gate + M3 cleanup
CHANGED FILES:
  runtime/etl_host.c
  runtime/etl_host.h
  runtime/etl_runtime.c
  runtime/etl_runtime.h
GATES RUN:
  make examples-cli: 0
  make examples: 0
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  make selfhost: 0
  make backend-vm: 0
  make vm-equivalence: 0
  scripts/triple_equiv_smoke.sh: 0
SUCCESS STATE:
  Verified that the Makefile examples-cli target explicitly depends on all
  three CLI smokes (scripts/cli_calculator_smoke.sh,
  scripts/cli_file_transform_smoke.sh, scripts/cli_config_rules_smoke.sh) and
  that scripts/examples_cli_smoke.sh covers hello, calculator, file_transform,
  and config_rules (syntax check path).  All gates exited 0 from a clean state.
  M3 tech debt closed: (1) replaced the stale once-per-process LIMITATION
  comment on etl_compile_module in runtime/etl_host.h and runtime/etl_host.c
  with accurate documentation — the implementation uses mkstemp temp files,
  not /dev/stdin, so no call-count limit applies; (2) removed the unused
  etl_write_record and etl_read_record helpers from runtime/etl_runtime.c and
  their declarations from runtime/etl_runtime.h.  Branch merged to main
  with --no-ff.
WHAT WAS LEFT UNDONE:
  None.
DISCOVERED ISSUES:
  None.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F3.4-examples-cli-gate.md
