FEATURE: F6.5-release-check-all-platforms
BRANCH: mission/etl-l5/F6.5-release-check-all-platforms
COMMITS:
  <sha> F6.5: orchestrate all M6 platform checks under release-check (VAL-DIST-005)
CHANGED FILES:
  Makefile
  docs/support-matrix.md
GATES RUN:
  bash scripts/setup.sh: 0
  make check: 0
  make release-check: 0
  make selfhost: 0
SUCCESS STATE:
  The top-level `make release-check` target in Makefile now depends on all five
  M6 platform sub-targets: release-check-x86_64 (Linux x86_64 native ELF smoke),
  release-check-aarch64 (Linux aarch64 cross-compiled + qemu smoke),
  release-check-macos (macOS x86_64+arm64 Mach-O build-validation), and
  release-check-wasm (WASI via wasmtime + browser-equivalent via Node.js
  WebAssembly API). Each sub-target remains independently runnable. The orchestration
  comment in Makefile (lines 198-209) documents each sub-target's scope.
  docs/support-matrix.md Platform/distribution table updated to add the
  browser-equivalent WASM row and the gate table entry for release-check now
  lists all five sub-targets. Confirmed exit 0 on dev workstation.
WHAT WAS LEFT UNDONE:
  Headless Chrome / Playwright path is not yet implemented (noted in setup.sh
  and docs/support-matrix.md as a gap). It is not wired into release-check-wasm
  because scripts/release_smoke_wasm_browser.sh uses the Node.js WebAssembly API
  as a browser-equivalent substitute per F6.4 design.
DISCOVERED ISSUES:
  None. All five sub-targets were already implemented by F6.1-F6.4; this feature
  only wired them together at the orchestration level.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F6.5-release-check-all-platforms.md
