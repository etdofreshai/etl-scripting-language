FEATURE: F4.1-fetch-sdl3
BRANCH: mission/etl-l5/F4.1-fetch-sdl3
COMMITS:
  d6e9747 F4.1: implement fetch_sdl3 — download, build, verify SDL3 release-3.4.8
  6826f96 Merge mission/etl-l5/F4.1-fetch-sdl3: SDL3 release-3.4.8 fetch+build into .deps/sdl3/
CHANGED FILES:
  .gitignore
  scripts/setup.sh
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0 (34/34 passed)
  make selfhost: 0
  git check-ignore .deps/sdl3: 0
  SDL3 verify compile+link (SDL_GetVersion probe): 0
SUCCESS STATE:
  SDL3 release-3.4.8 is now fetched and built locally on demand. scripts/setup.sh
  implements fetch_sdl3() which downloads SDL3-3.4.8.tar.gz from GitHub
  (https://github.com/libsdl-org/SDL/releases/download/release-3.4.8/SDL3-3.4.8.tar.gz),
  extracts to .deps/sdl3-src/, builds with cmake into .deps/sdl3/ (headers at
  .deps/sdl3/include/SDL3/SDL.h, shared lib at .deps/sdl3/lib/libSDL3.so).
  verify_sdl3() compiles and runs a probe using SDL_GetVersion(); confirmed
  output "SDL3 version: 3.4.8". .deps/ is gitignored (confirmed via
  git check-ignore). The build is idempotent — re-running skips when the
  sentinel .deps/sdl3/include/SDL3/SDL.h exists. rpath is baked in at build
  time via -Wl,-rpath so binaries do not require manual LD_LIBRARY_PATH export
  (though that path is documented in setup.sh output for manual use). All
  baseline gates pass unchanged.
WHAT WAS LEFT UNDONE:
  - F4.2/F4.3 graphics smokes not yet updated to use the new SDL3 install
    (deferred per feature spec).
  - Static-only linking option not exposed as a flag (shared lib sufficient
    for M4 goals).
DISCOVERED ISSUES:
  - SDL_Init(SDL_INIT_VIDEO) would fail in the headless CI environment (no
    display), so the verification probe uses SDL_GetVersion() instead, which
    succeeds unconditionally. This is the documented approach in the feature
    spec ("probably fails but compiles+links — that's enough").
  - PkgConfig was not found on the build host (cmake warning "Could NOT find
    PkgConfig"); SDL3 cmake build proceeded without it and completed successfully.
PROVIDER:
  primary=glm, fallbacks=[]
SDL3_VERSION_PINNED: release-3.4.8 (3.4.8)
VERIFICATION_RESULT:
  cc -I.deps/sdl3/include .deps/sdl3_check.c -L.deps/sdl3/lib -lSDL3 -Wl,-rpath,.deps/sdl3/lib -o /tmp/etl_sdl3_check
  /tmp/etl_sdl3_check → stdout: "SDL3 version: 3.4.8" exit code: 0
HANDOFF FILE:
  .missions/etl-l5/handoffs/F4.1-fetch-sdl3.md
