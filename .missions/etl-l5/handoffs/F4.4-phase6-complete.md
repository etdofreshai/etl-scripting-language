FEATURE: F4.4-phase6-complete
BRANCH: mission/etl-l5/F4.4-phase6-complete
COMMITS:
  b3fe03a Add Conway's Life golden (wave 6e) and fix visual_smoke SDL3 detection
  c060f15 Merge mission/etl-l5/F4.4-phase6-complete (wave 6e Life golden + SDL3 live smoke)
CHANGED FILES:
  examples/visual/life.etl
  examples/visual/life.golden.ppm
  scripts/life_golden_smoke.sh
  scripts/visual_smoke.sh
  docs/PATH_TO_APPS.md
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  scripts/life_golden_smoke.sh: 0
  scripts/visual_smoke.sh: 0
  make visual: 0
  make examples: 0
  make selfhost: 0
SUCCESS STATE:
  Wave 6e (Conway's Life golden) is complete. examples/visual/life.etl implements
  Conway's Life on a 32x32 grid with a deterministic blinker seed (horizontal,
  cells 15,16,17 on row 16), running 10 generations and writing the final frame as
  a PPM via the software graphics backend. The golden is committed at
  examples/visual/life.golden.ppm. scripts/life_golden_smoke.sh builds the binary,
  runs it twice, and byte-compares both outputs against the golden; it is wired
  into scripts/visual_smoke.sh so that make visual covers it. visual_smoke.sh was
  updated to detect SDL3 via .deps/sdl3/include/SDL3/SDL.h instead of pkg-config,
  so the live SDL3 bouncing-rect smoke now runs rather than SKIPping. docs/PATH_TO_APPS.md
  marks 6e complete, updates Current Selection to 7a (Calculator), and notes that
  6d (audio stub) was deferred by supervisor reorder.
WHAT WAS LEFT UNDONE:
  Wave 6d (deterministic audio runtime stub) was not implemented. The supervisor
  feature spec reordered Phase 6 to deliver 6e first for earlier graphical payoff;
  6d remains open and may be scheduled between 7a and 7b or deferred further.
DISCOVERED ISSUES:
  - PATH_TO_APPS.md listed wave 6d as "Deterministic audio runtime stub" (not
    scripted input). The feature spec incorrectly referenced 6d as scripted input,
    but scripted input was actually wave 6c. This was noted and PATH_TO_APPS.md
    updated accordingly.
  - ETL v0 does not allow integer array locals to have initializer expressions
    (= 0 is rejected). Arrays are zero-initialized by default; initializers must
    be omitted. Fixed during implementation.
  - Non-void user-defined function calls cannot be used as standalone expression
    statements in ETL v0. Resolved by assigning call results to a pre-declared
    integer variable (rc) in main().
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F4.4-phase6-complete.md
