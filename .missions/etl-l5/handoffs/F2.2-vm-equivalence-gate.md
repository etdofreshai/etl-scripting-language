FEATURE: F2.2-vm-equivalence-gate
BRANCH: mission/etl-l5/F2.2-vm-equivalence-gate
COMMITS:
  04ad13e Add make vm-equivalence target and smoke script
  7e68f1c Merge mission/etl-l5/F2.2-vm-equivalence-gate: add vm-equivalence gate
CHANGED FILES:
  Makefile
  runtime/etl_vm_main.c
  scripts/vm_equivalence_smoke.sh
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  make selfhost: 0
  make backend-vm: 0
  make vm-equivalence: 0
SUCCESS STATE:
  Shipped `make vm-equivalence` gate backed by scripts/vm_equivalence_smoke.sh. The
  script builds a C stdin driver (runtime/etl_vm_main.c) wrapping etl_vm_run_main_i32,
  compiles the compiler1 bytecode pipeline, and runs 21 corpus fixtures through both
  the C oracle VM and bin/etl-vm-etl, asserting identical exit codes. All 21 pass.
  The Makefile target depends on bin/etl-vm-etl and delegates to the smoke script.
  Merged to main as 7e68f1c with --no-ff; post-merge vm-equivalence gate exits 0.
WHAT WAS LEFT UNDONE:
  Three fixtures excluded from the gate due to emit_bytecode limitations:
    - ret_unary_minus: unary-minus literal not supported in bytecode emitter
    - local_bool: bool local variable type not supported in bytecode emitter
    - local_i8: i8 scalar local / i8 array indexing not supported in bytecode emitter
  These are pre-existing F1.x limitations unrelated to this feature.
DISCOVERED ISSUES:
  None. All 21 selected fixtures produced identical exit codes from both VMs on first
  run. The three excluded fixtures fail at the emit_bytecode stage (bc_driver exits 14
  meaning emit_bytecode returned negative), so they do not represent a VM divergence.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F2.2-vm-equivalence-gate.md

Per-fixture pass/fail counts:
  PASS ret_literal (exit 42)
  PASS ret_add (exit 30)
  PASS ret_mul (exit 42)
  PASS ret_arith_complex (exit 11)
  PASS ret_nested (exit 21)
  PASS ret_div_mod (exit 7)
  PASS ret_complex (exit 21)
  PASS let_simple (exit 42)
  PASS let_arith (exit 42)
  PASS let_chain (exit 21)
  PASS assign_local (exit 7)
  PASS multi_fn_basic (exit 7)
  PASS multi_fn_chain (exit 11)
  PASS fn_params_two (exit 42)
  PASS fn_recursive (exit 55)
  PASS local_bool_expr (exit 99)
  PASS heap_alloc_basic (exit 0)
  PASS string_heap_basic (exit 0)
  PASS dynarr_basic (exit 0)
  PASS tagged_union_basic (exit 0)
  PASS large_bytecode (exit 42)
  SKIP ret_unary_minus (emit_bytecode limitation: unary-minus literal)
  SKIP local_bool (emit_bytecode limitation: bool local type)
  SKIP local_i8 (emit_bytecode limitation: i8 scalar/array)
  Summary: 21 passed, 0 failed, 3 skipped
