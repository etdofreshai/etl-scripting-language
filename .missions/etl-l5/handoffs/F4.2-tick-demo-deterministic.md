FEATURE: F4.2-tick-demo-deterministic
BRANCH: mission/etl-l5/F4.2-tick-demo-deterministic
COMMITS:
  d8daa2f F4.2: add 3-run determinism check for tick_demo in visual_smoke.sh
CHANGED FILES:
  scripts/visual_smoke.sh
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0
  scripts/visual_smoke.sh: 0
  make visual: 0
  make examples: 0
  make selfhost: 0
SUCCESS STATE:
  tick_demo.etl (pure arithmetic, no entropy sources) was confirmed deterministic at exit code 5 across 3 consecutive runs, matching tick_demo.expected byte-for-byte. A new check_tick_demo_deterministic() function was added to scripts/visual_smoke.sh that compiles the demo once and executes it three times, failing loudly if any run deviates from the expected exit value. No changes were needed to tick_demo.etl or tick_demo.expected. The feature branch was merged to main with --no-ff.
WHAT WAS LEFT UNDONE:
  None. The feature was already implicitly satisfied by the deterministic nature of the demo; the 3-run gate is now explicit and wired.
DISCOVERED ISSUES:
  None. tick_demo.etl had no wall-clock, no random seeding, and no system entropy — purely deterministic arithmetic.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F4.2-tick-demo-deterministic.md
