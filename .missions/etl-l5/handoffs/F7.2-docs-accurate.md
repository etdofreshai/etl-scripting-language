FEATURE: F7.2-docs-accurate
BRANCH: mission/etl-l5/F7.2-docs-accurate
COMMITS:
  ae81612 F7.2: fix stale facts in SPEC, support-matrix, and backend-plan docs
  ecba731 F7.2: update ROADMAP and language-goal-roadmap to reflect L5 shipped state
  d644630 Merge mission/etl-l5/F7.2-docs-accurate: docs accuracy sweep for M7-release (VAL-REL-002)
CHANGED FILES:
  docs/SPEC.md
  docs/support-matrix.md
  docs/backend-plan.md
  docs/language-goal-roadmap.md
  docs/ROADMAP.md
  .missions/etl-l5/handoffs/F6.5-release-check-all-platforms.md
GATES RUN:
  make check: 0
  scripts/c1_equiv_smoke.sh: 0 (34/34 passed)
SUCCESS STATE:
  All eight target docs now describe shipped state, not aspirational state.
  Specific fixes: (1) SPEC.md — 1024-byte bytecode buffer references corrected
  to 64 KB (raised in F2.0); M2 VM-in-ETL noted as shipped, not future;
  etlval_str VM fixture note updated. (2) support-matrix.md — bytecode buffer
  corrected to 64 KB; Limitations section updated: c1 fixed point complete,
  c0 frozen, VM-in-ETL (M2) shipped with host bridge. (3) backend-plan.md —
  Current State corrected: c1 fixed point complete; "No runtime ETL VM exists
  yet" replaced with accurate description of shipped vm.etl and host bridge;
  ETL VM bytecode backend table row updated to reflect vm.etl status. (4)
  language-goal-roadmap.md — Current Baseline replaced with full M7-release
  gate status; Readiness Level status markers added (L0 COMPLETE, L1 COMPLETE,
  L2 PARTIAL, L3 COMPLETE, L4 COMPLETE, L5 PARTIAL with explicit gaps);
  Workstream A and C updated to reflect completion. (5) ROADMAP.md — Phase 5
  status row updated to done; Phase 6 status row updated to done (6d deferred);
  Phase 5 status note updated to COMPLETE; sub-tasks 5f/5g marked DONE;
  L5 Mission Status section added with M0-M7 milestone table and remaining
  future work list. DESIGN.md and PATH_TO_APPS.md were audited and found
  accurate (DESIGN.md §37b already correctly documents the 64 KB buffer and
  M2; PATH_TO_APPS.md correctly marks 6d as deferred and 6e as done).
WHAT WAS LEFT UNDONE:
  - DESIGN.md and PATH_TO_APPS.md: no changes needed; already accurate.
  - runtime-vm-plan.md: describes the VM build plan phases; these are
    aspirational/planning phases that served as design, not claims about
    current state; no inaccurate claims found.
  - docs/ROADMAP.md Phase 7/8/9 rows remain "not started" — accurate.
  - docs/language-goal-roadmap.md Workstreams B/D/E not updated (tasks
    described there are ongoing/future; no false completions claimed).
DISCOVERED ISSUES:
  - runtime-vm-plan.md §Current State (bytecode format reference table,
    Phase 4) documents only L/L=; store/load local opcodes; but vm.etl
    (shipped in M2) also implements function call frames (C;/J;/etc.) and
    M1 opaque-type opcodes (HS*/HD*/HV*/HA;/HF;). The runtime-vm-plan.md
    bytecode format table is incomplete relative to the shipped vm.etl.
    This is a docs gap but not an inaccuracy (the table covers the base subset).
    Tracked as potential follow-up in F7.3 or post-M7.
  - support-matrix.md `selfhost-selfcompile` gate says "c1 emits its own
    source as 112,575 bytes of valid C" — this byte count may drift as the
    compiler grows. No gate verifies this count; it is an informational note.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F7.2-docs-accurate.md
