FEATURE: F6.2-linux-aarch64-qemu
BRANCH: mission/etl-l5/F6.2-linux-aarch64-qemu
COMMITS:
  1cddf37 F6.2: add Linux aarch64 cross-compile + qemu smoke (VAL-DIST-002)
  2e6da2f Merge mission/etl-l5/F6.2-linux-aarch64-qemu (VAL-DIST-002)
CHANGED FILES:
  scripts/setup.sh
  scripts/build_release_tarball_aarch64.sh
  scripts/release_smoke_aarch64.sh
  Makefile
GATES RUN:
  bash scripts/setup.sh: 0
  make check: 0
  make release-check-aarch64: 0
  make release-check: 0
SUCCESS STATE:
  VAL-DIST-002 is satisfied. scripts/setup.sh now implements fetch_qemu_aarch64()
  (pinned multiarch/qemu-user-static v7.2.0-1, fetched to .deps/qemu-aarch64-static)
  and fetch_zig() (pinned zig 0.14.1, fetched to .deps/zig/). The new
  scripts/build_release_tarball_aarch64.sh cross-compiles etl-vm-etl to
  aarch64-linux-musl via zig cc and stages build/release/etl-linux-aarch64.tar.gz.
  scripts/release_smoke_aarch64.sh cross-compiles 7 c1 corpus fixtures (ret_literal,
  let_simple, let_arith, ret_add, fn_params_two, ret_mul, assign_local) to aarch64-musl
  via zig cc and runs each under .deps/qemu-aarch64-static; all 7 exit codes match
  expected. Makefile target release-check-aarch64 added. make release-check (x86_64
  path) remains green.
WHAT WAS LEFT UNDONE:
  - release-check does not yet include release-check-aarch64 (deferred to F6.5
    per the feature spec: "doesn't have to include aarch64 yet — F6.5's job").
  - The aarch64 tarball's bundled bin/etl launcher requires python3 and a native
    aarch64 cc at untar time to compile ETL programs; etl-vm-etl only runs
    pre-compiled bytecode. Full native compile on aarch64 is out of scope here.
DISCOVERED ISSUES:
  - No issues. The multiarch/qemu-user-static v7.2.0-1 binary fetches cleanly as a
    single ~10 MB file. Zig 0.14.1 extracts into zig-x86_64-linux-0.14.1/ and
    the mv pattern works without issues.
  - Recovery path for qemu fetch failure is documented in setup.sh inline comments.
PROVIDER:
  primary=glm, fallbacks=[]
HANDOFF FILE:
  .missions/etl-l5/handoffs/F6.2-linux-aarch64-qemu.md

PINNED VERSIONS:
  qemu-user-static: v7.2.0-1 (multiarch/qemu-user-static GitHub release)
    URL: https://github.com/multiarch/qemu-user-static/releases/download/v7.2.0-1/qemu-aarch64-static
  zig: 0.14.1 (ziglang.org)
    URL: https://ziglang.org/download/0.14.1/zig-x86_64-linux-0.14.1.tar.xz
