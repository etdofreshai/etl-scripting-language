# ETL V-zero Bootstrap Overview

V-zero is the public bootstrap stage for ETL.

It exists to seed the real ETL compiler and should remain intentionally small.
The bootstrap host implementation is Rust-first and Linux-first.
